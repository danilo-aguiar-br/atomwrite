{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "completions",
  "description": "NDJSON/agent envelope for `completions` (G-042 minimal published schema; additionalProperties allowed for evolution).",
  "type": "object",
  "additionalProperties": true
}
